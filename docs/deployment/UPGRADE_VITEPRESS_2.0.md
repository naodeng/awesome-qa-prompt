# VitePress 2.0 升级指南

## 📋 升级步骤

### 1. 安装 VitePress 2.0

```bash
npm install vitepress@^2.0.0
```

或者使用 yarn：

```bash
yarn add vitepress@^2.0.0
```

### 2. 验证安装

```bash
npm list vitepress
```

应该显示 `vitepress@2.0.x`

### 3. 测试构建

```bash
npm run docs:build
npm run docs:dev
```

## 🔍 搜索功能配置

搜索功能已在配置中启用，使用本地搜索（local search）提供者。

### 配置位置

- 中文搜索配置：`.vitepress/config.mts` → `locales.root.themeConfig.search`
- 英文搜索配置：`.vitepress/config.mts` → `locales.en.themeConfig.search`

### 搜索功能特性

- ✅ **本地搜索**：无需外部服务，完全在浏览器中运行
- ✅ **全文搜索**：支持标题、内容、标题层级搜索
- ✅ **模糊匹配**：支持拼写容错
- ✅ **中英文支持**：完整的中英文搜索界面翻译

### 搜索按钮位置

搜索按钮会自动显示在导航栏中，位于：
- Logo 和导航菜单之间
- 语言切换按钮之前

### 如果搜索按钮不显示

1. **检查配置**：确保 `search.provider: 'local'` 已设置
2. **清除缓存**：删除 `node_modules` 和 `.vitepress/dist`，重新安装和构建
3. **检查控制台**：查看浏览器控制台是否有错误信息

## ⚠️ 注意事项

### 破坏性变更

VitePress 2.0 可能包含一些破坏性变更，请参考：
- [VitePress 官方迁移指南](https://vitepress.dev/guide/migration-from-vitepress-0)

### 兼容性检查

升级后请检查：
- ✅ 构建是否成功
- ✅ 搜索功能是否正常
- ✅ 所有页面是否正常显示
- ✅ 路由是否正常工作

## 🐛 故障排除

### 问题：搜索按钮不显示

**解决方案**：
1. 确保配置格式正确（使用 `locales.root` 和 `locales.en`）
2. 检查 CSS 是否隐藏了搜索按钮
3. 清除浏览器缓存并重新加载

### 问题：搜索无结果

**解决方案**：
1. 确保所有页面都有正确的 frontmatter（title）
2. 检查构建输出中是否生成了搜索索引
3. 查看浏览器控制台是否有 JavaScript 错误

### 问题：构建失败

**解决方案**：
1. 检查 Node.js 版本（需要 >= 18.0.0）
2. 删除 `node_modules` 和 `package-lock.json`，重新安装
3. 查看构建日志中的具体错误信息

## 📝 升级检查清单

- [ ] 备份当前配置
- [ ] 更新 `package.json` 中的 VitePress 版本
- [ ] 运行 `npm install`
- [ ] 测试本地开发服务器
- [ ] 测试构建过程
- [ ] 验证搜索功能
- [ ] 检查所有页面显示
- [ ] 测试部署流程

## 🔗 相关链接

- [VitePress 官方文档](https://vitepress.dev/)
- [VitePress 搜索功能文档](https://vitepress.dev/reference/default-theme-search)
- [VitePress GitHub 仓库](https://github.com/vuejs/vitepress)
