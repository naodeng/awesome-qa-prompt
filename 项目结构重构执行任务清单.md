# 项目结构重构执行任务清单

**基于文档**: 项目结构优化方案详细设计.md  
**执行方案**: 方案 A - 渐进式优化  
**预计工作量**: 8-12 人天  
**预计时间**: 2-3 周  
**创建日期**: 2025-01-16

---

## 📋 任务概览

本任务清单将项目结构重构分为 **5个阶段**，**18个主要任务**，**50+个具体步骤**。

### 阶段概览

| 阶段 | 任务数 | 工作量 | 时间 | 状态 |
|------|--------|--------|------|------|
| 🔧 准备阶段 | 3 | 1-2 人天 | 1-2 天 | ⏳ 待开始 |
| 🗂️ 执行阶段 | 5 | 3-5 人天 | 3-5 天 | ⏳ 待开始 |
| ⚙️ 配置更新阶段 | 3 | 2-3 人天 | 2-3 天 | ⏳ 待开始 |
| 🧪 测试阶段 | 3 | 2-3 人天 | 2-3 天 | ⏳ 待开始 |
| 🚀 发布阶段 | 4 | 1 人天 | 1 天 | ⏳ 待开始 |

---

## 🔧 阶段 1：准备阶段（1-2天）

### 任务 1.1：项目备份和分支管理

**目标**: 确保项目安全，创建工作分支

**优先级**: 🔥 高  
**预计时间**: 2-3 小时  
**负责人**: 开发者 A

#### 具体步骤：

1. **创建备份分支**
   ```bash
   git checkout -b backup/before-restructure
   git push origin backup/before-restructure
   ```
   - [ ] 备份分支已创建
   - [ ] 备份分支已推送到远程

2. **创建本地备份**
   ```bash
   tar -czf awesome-qa-prompt-backup-$(date +%Y%m%d).tar.gz .
   ```
   - [ ] 本地备份文件已创建
   - [ ] 备份文件大小合理（检查是否包含所有文件）

3. **创建工作分支**
   ```bash
   git checkout main
   git checkout -b feature/directory-restructure
   ```
   - [ ] 工作分支已创建
   - [ ] 当前在工作分支上

**验收标准**:
- ✅ 备份分支存在且包含完整代码
- ✅ 本地备份文件存在且可用
- ✅ 工作分支已创建并切换

---

### 任务 1.2：准备迁移脚本

**目标**: 创建自动化迁移脚本

**优先级**: 🔥 高  
**预计时间**: 3-4 小时  
**负责人**: 开发者 A

#### 具体步骤：

1. **创建脚本目录**
   ```bash
   mkdir -p scripts/migrate
   ```
   - [ ] scripts/migrate 目录已创建

2. **创建删除脚本** (`scripts/migrate/01-delete-empty-dirs.sh`)
   ```bash
   #!/bin/bash
   set -e
   echo "🗑️  删除空目录和系统文件..."
   
   # 删除 articles 目录
   if [ -d "articles" ]; then
       rm -rf articles/
       echo "✅ 已删除 articles/ 目录"
   fi
   
   # 删除 .DS_Store 文件
   if [ -f ".DS_Store" ]; then
       rm .DS_Store
       echo "✅ 已删除 .DS_Store 文件"
   fi
   
   # 更新 .gitignore
   if ! grep -q ".DS_Store" .gitignore; then
       echo ".DS_Store" >> .gitignore
       echo "**/.DS_Store" >> .gitignore
       echo "✅ 已更新 .gitignore"
   fi
   
   echo "✅ 删除操作完成"
   ```
   - [ ] 删除脚本已创建
   - [ ] 脚本权限已设置 (`chmod +x`)

3. **创建重命名脚本** (`scripts/migrate/02-rename-testing-types.sh`)
   ```bash
   #!/bin/bash
   set -e
   echo "📝 重命名测试类型模块..."
   
   # 定义重命名映射
   declare -A rename_map=(
       ["AccessibilityTesting"]="accessibility-testing"
       ["AIAssistedTesting"]="ai-assisted-testing"
       ["APITesting"]="api-testing"
       ["AutomationTesting"]="automation-testing"
       ["BugReporting"]="bug-reporting"
       ["FunctionalTesting"]="functional-testing"
       ["ManualTesting"]="manual-testing"
       ["MobileTesting"]="mobile-testing"
       ["PerformanceTesting"]="performance-testing"
       ["RequirementsAnalysis"]="requirements-analysis"
       ["SecurityTesting"]="security-testing"
       ["TestCaseReviewer"]="test-case-reviewer"
       ["TestCaseWriting"]="test-case-writing"
       ["TestReporting"]="test-reporting"
       ["TestStrategy"]="test-strategy"
   )
   
   # 执行重命名
   for old_name in "${!rename_map[@]}"; do
       new_name="${rename_map[$old_name]}"
       if [ -d "$old_name" ]; then
           mv "$old_name" "$new_name"
           echo "✅ $old_name -> $new_name"
       fi
   done
   
   # 重命名 Workflows
   if [ -d "Workflows" ]; then
       mv Workflows workflows
       echo "✅ Workflows -> workflows"
   fi
   
   echo "✅ 重命名操作完成"
   ```
   - [ ] 重命名脚本已创建
   - [ ] 脚本权限已设置

4. **创建目录结构脚本** (`scripts/migrate/03-create-new-structure.sh`)
   ```bash
   #!/bin/bash
   set -e
   echo "📁 创建新目录结构..."
   
   # 创建主要目录
   mkdir -p docs/guides
   mkdir -p docs/deployment
   mkdir -p docs/contributing
   mkdir -p docs/analysis
   mkdir -p testing-types
   mkdir -p prompt-frameworks/frameworks
   mkdir -p examples/basic-usage
   mkdir -p examples/advanced-usage
   mkdir -p examples/best-practices
   
   echo "✅ 新目录结构创建完成"
   ```
   - [ ] 目录结构脚本已创建
   - [ ] 脚本权限已设置

5. **创建文件移动脚本** (`scripts/migrate/04-move-files.sh`)
   ```bash
   #!/bin/bash
   set -e
   echo "📦 移动文件到新位置..."
   
   # 移动部署文档
   [ -f "QUICK_DEPLOY.md" ] && mv QUICK_DEPLOY.md docs/deployment/
   [ -f "QUICK_DEPLOY_EN.md" ] && mv QUICK_DEPLOY_EN.md docs/deployment/
   
   # 移动分析报告
   [ -f "项目现状分析报告.md" ] && mv 项目现状分析报告.md docs/analysis/
   [ -f "项目改进方案总结.md" ] && mv 项目改进方案总结.md docs/analysis/
   [ -f "目录结构深度分析.md" ] && mv 目录结构深度分析.md docs/analysis/
   [ -f "项目结构优化方案详细设计.md" ] && mv 项目结构优化方案详细设计.md docs/analysis/
   
   # 移动首页文件
   [ -f "index.md" ] && mv index.md docs/
   [ -f "en.md" ] && mv en.md docs/
   
   # 移动测试类型模块到 testing-types
   for dir in accessibility-testing ai-assisted-testing api-testing automation-testing bug-reporting functional-testing manual-testing mobile-testing performance-testing requirements-analysis security-testing test-case-reviewer test-case-writing test-reporting test-strategy; do
       if [ -d "$dir" ]; then
           mv "$dir" testing-types/
           echo "✅ 已移动 $dir 到 testing-types/"
       fi
   done
   
   # 处理 Base 目录
   if [ -d "Base/PromptExample" ]; then
       mv Base/PromptExample/* prompt-frameworks/frameworks/
       rm -rf Base/
       echo "✅ 已重构 Base 目录为 prompt-frameworks"
   fi
   
   echo "✅ 文件移动完成"
   ```
   - [ ] 文件移动脚本已创建
   - [ ] 脚本权限已设置

6. **创建 Reference 处理脚本** (`scripts/migrate/05-integrate-reference.sh`)
   ```bash
   #!/bin/bash
   set -e
   echo "🔄 处理 Reference 目录..."
   
   # 删除完全重复的 workflows
   [ -d "Reference/workflows" ] && rm -rf Reference/workflows/
   
   # 移动独立内容到 guides
   [ -d "Reference/cursor-ai" ] && mv Reference/cursor-ai docs/guides/
   [ -d "Reference/specialized-testing" ] && mv Reference/specialized-testing docs/guides/specialized
   
   # 提示手动处理重复内容
   if [ -d "Reference" ]; then
       echo "⚠️  请手动审查并整合以下目录的内容："
       ls -la Reference/
       echo "建议将内容整合到对应的 testing-types 模块中"
       echo "完成后可以删除 Reference 目录"
   fi
   
   echo "✅ Reference 目录处理完成"
   ```
   - [ ] Reference 处理脚本已创建
   - [ ] 脚本权限已设置

**验收标准**:
- ✅ 所有迁移脚本已创建
- ✅ 脚本具有执行权限
- ✅ 脚本语法正确（可以运行 `bash -n script.sh` 检查）

---

### 任务 1.3：环境准备和依赖检查

**目标**: 确保执行环境就绪

**优先级**: ⭐ 中  
**预计时间**: 1 小时  
**负责人**: 开发者 A

#### 具体步骤：

1. **检查 Node.js 环境**
   ```bash
   node --version  # 应该是 20+
   npm --version
   ```
   - [ ] Node.js 版本 >= 20
   - [ ] npm 可用

2. **安装项目依赖**
   ```bash
   npm ci
   ```
   - [ ] 依赖安装成功
   - [ ] 无错误或警告

3. **测试构建**
   ```bash
   npm run docs:build
   ```
   - [ ] 构建成功
   - [ ] 无错误

4. **安装额外工具**
   ```bash
   npm install -g markdown-link-check markdownlint-cli
   ```
   - [ ] markdown-link-check 已安装
   - [ ] markdownlint-cli 已安装

**验收标准**:
- ✅ 开发环境完整可用
- ✅ 项目可以正常构建
- ✅ 必要工具已安装

---

## 🗂️ 阶段 2：执行阶段（3-5天）

### 任务 2.1：删除空目录和系统文件

**目标**: 清理无用的目录和文件

**优先级**: 🔥 高  
**预计时间**: 30 分钟  
**负责人**: 开发者 A

#### 具体步骤：

1. **执行删除脚本**
   ```bash
   bash scripts/migrate/01-delete-empty-dirs.sh
   ```
   - [ ] 脚本执行成功
   - [ ] articles/ 目录已删除
   - [ ] .DS_Store 文件已删除
   - [ ] .gitignore 已更新

2. **验证删除结果**
   ```bash
   ls -la | grep articles  # 应该没有输出
   ls -la | grep .DS_Store  # 应该没有输出
   grep -q ".DS_Store" .gitignore && echo "✅ .gitignore 已更新"
   ```
   - [ ] articles 目录不存在
   - [ ] .DS_Store 文件不存在
   - [ ] .gitignore 包含 .DS_Store 规则

**验收标准**:
- ✅ 所有空目录已删除
- ✅ 所有系统文件已删除
- ✅ .gitignore 已正确更新

---

### 任务 2.2：重命名测试类型模块

**目标**: 统一目录命名为 kebab-case

**优先级**: 🔥 高  
**预计时间**: 1 小时  
**负责人**: 开发者 A

#### 具体步骤：

1. **执行重命名脚本**
   ```bash
   bash scripts/migrate/02-rename-testing-types.sh
   ```
   - [ ] 脚本执行成功
   - [ ] 所有测试类型目录已重命名

2. **验证重命名结果**
   ```bash
   # 检查新目录是否存在
   ls -d accessibility-testing ai-assisted-testing api-testing automation-testing bug-reporting functional-testing manual-testing mobile-testing performance-testing requirements-analysis security-testing test-case-reviewer test-case-writing test-reporting test-strategy workflows
   ```
   - [ ] 所有新目录名存在
   - [ ] 旧目录名不存在
   - [ ] 目录内文件完整

3. **检查文件完整性**
   ```bash
   # 随机检查几个目录的文件
   ls functional-testing/
   ls ai-assisted-testing/
   ```
   - [ ] 文件数量正确
   - [ ] 文件内容未损坏

**验收标准**:
- ✅ 所有测试类型目录已重命名为 kebab-case
- ✅ Workflows 目录已重命名为 workflows
- ✅ 目录内文件完整无缺失

---

### 任务 2.3：创建新目录结构

**目标**: 建立新的项目组织结构

**优先级**: 🔥 高  
**预计时间**: 30 分钟  
**负责人**: 开发者 A

#### 具体步骤：

1. **执行目录创建脚本**
   ```bash
   bash scripts/migrate/03-create-new-structure.sh
   ```
   - [ ] 脚本执行成功

2. **验证目录结构**
   ```bash
   tree -d -L 3  # 查看目录结构
   ```
   - [ ] docs/ 目录及子目录已创建
   - [ ] testing-types/ 目录已创建
   - [ ] prompt-frameworks/ 目录及子目录已创建
   - [ ] examples/ 目录及子目录已创建

3. **创建必要的 README 文件**
   - [ ] docs/README.md 已创建
   - [ ] examples/README.md 已创建
   - [ ] prompt-frameworks/README.md 已创建

**验收标准**:
- ✅ 所有新目录已创建
- ✅ 目录结构符合设计方案
- ✅ 必要的 README 文件已创建

---

### 任务 2.4：移动文件到新位置

**目标**: 将文件移动到新的目录结构中

**优先级**: 🔥 高  
**预计时间**: 2-3 小时  
**负责人**: 开发者 A

#### 具体步骤：

1. **执行文件移动脚本**
   ```bash
   bash scripts/migrate/04-move-files.sh
   ```
   - [ ] 脚本执行成功

2. **验证文档文件移动**
   ```bash
   ls docs/deployment/
   ls docs/analysis/
   ls docs/
   ```
   - [ ] 部署文档已移动到 docs/deployment/
   - [ ] 分析报告已移动到 docs/analysis/
   - [ ] 首页文件已移动到 docs/

3. **验证测试模块移动**
   ```bash
   ls testing-types/
   ```
   - [ ] 所有测试类型模块已移动到 testing-types/
   - [ ] 模块内文件完整

4. **验证 Base 目录重构**
   ```bash
   ls prompt-frameworks/frameworks/
   ```
   - [ ] Base/PromptExample 内容已移动到 prompt-frameworks/frameworks/
   - [ ] Base 目录已删除

**验收标准**:
- ✅ 所有文档文件已移动到正确位置
- ✅ 所有测试模块已移动到 testing-types/
- ✅ Base 目录已成功重构为 prompt-frameworks

---

### 任务 2.5：处理 Reference 目录

**目标**: 整合或删除 Reference 目录内容

**优先级**: ⭐ 中  
**预计时间**: 2-3 小时  
**负责人**: 开发者 A + 开发者 B

#### 具体步骤：

1. **执行 Reference 处理脚本**
   ```bash
   bash scripts/migrate/05-integrate-reference.sh
   ```
   - [ ] 脚本执行成功

2. **手动审查重复内容**
   - [ ] 审查 Reference/ai-assisted-qa/ 与 testing-types/ai-assisted-testing/ 的重复内容
   - [ ] 审查 Reference/automation-qa/ 与 testing-types/automation-testing/ 的重复内容
   - [ ] 审查 Reference/manual-qa/ 与 testing-types/manual-testing/ 的重复内容
   - [ ] 审查 Reference/mobile-testing/ 与 testing-types/mobile-testing/ 的重复内容

3. **整合有价值的内容**
   - [ ] 将有价值的独特内容整合到对应模块
   - [ ] 删除完全重复的内容
   - [ ] 记录整合过程和决策

4. **验证独立内容移动**
   ```bash
   ls docs/guides/
   ```
   - [ ] cursor-ai 内容已移动到 docs/guides/
   - [ ] specialized-testing 内容已移动到 docs/guides/specialized/

5. **删除 Reference 目录**
   ```bash
   rm -rf Reference/
   ```
   - [ ] Reference 目录已完全删除

**验收标准**:
- ✅ 重复内容已删除
- ✅ 有价值的内容已整合到对应模块
- ✅ 独立内容已移动到 docs/guides/
- ✅ Reference 目录已删除

---

## ⚙️ 阶段 3：配置更新阶段（2-3天）

### 任务 3.1：更新 VitePress 配置

**目标**: 更新所有路径配置以适应新结构

**优先级**: 🔥 高  
**预计时间**: 3-4 小时  
**负责人**: 开发者 A

#### 具体步骤：

1. **备份当前配置**
   ```bash
   cp .vitepress/config.mts .vitepress/config.mts.backup
   ```
   - [ ] 配置文件已备份

2. **更新基础配置**
   - 更新 `srcDir` 配置（如果需要）
   - 更新 `outDir` 配置（如果需要）
   - [ ] 基础配置已更新

3. **更新侧边栏配置**
   ```typescript
   sidebar: {
     '/testing-types/': [
       {
         text: '测试类型',
         items: [
           { text: '需求分析', link: '/testing-types/requirements-analysis/' },
           { text: '测试用例编写', link: '/testing-types/test-case-writing/' },
           { text: '功能测试', link: '/testing-types/functional-testing/' },
           { text: '性能测试', link: '/testing-types/performance-testing/' },
           { text: '自动化测试', link: '/testing-types/automation-testing/' },
           { text: '移动端测试', link: '/testing-types/mobile-testing/' },
           { text: '缺陷上报', link: '/testing-types/bug-reporting/' },
           { text: '测试报告', link: '/testing-types/test-reporting/' },
           { text: '测试策略', link: '/testing-types/test-strategy/' },
           { text: 'AI辅助测试', link: '/testing-types/ai-assisted-testing/' },
           { text: '手动测试', link: '/testing-types/manual-testing/' },
           { text: '安全测试', link: '/testing-types/security-testing/' },
           { text: 'API测试', link: '/testing-types/api-testing/' },
           { text: '可访问性测试', link: '/testing-types/accessibility-testing/' },
           { text: '测试用例评审', link: '/testing-types/test-case-reviewer/' }
         ]
       }
     ],
     '/workflows/': [
       {
         text: '工作流程',
         items: [
           { text: '日常测试工作流程', link: '/workflows/daily-testing-workflow' },
           { text: '迭代测试工作流程', link: '/workflows/sprint-testing-workflow' },
           { text: '发布测试工作流程', link: '/workflows/release-testing-workflow' }
         ]
       }
     ],
     '/prompt-frameworks/': [
       {
         text: '提示词框架',
         items: [
           { text: '框架介绍', link: '/prompt-frameworks/' },
           { text: '框架列表', link: '/prompt-frameworks/frameworks/' }
         ]
       }
     ],
     '/examples/': [
       {
         text: '使用示例',
         items: [
           { text: '基础使用', link: '/examples/basic-usage/' },
           { text: '高级使用', link: '/examples/advanced-usage/' },
           { text: '最佳实践', link: '/examples/best-practices/' }
         ]
       }
     ]
   }
   ```
   - [ ] 侧边栏配置已更新
   - [ ] 所有路径正确

4. **更新导航菜单**
   ```typescript
   nav: [
     { text: '首页', link: '/' },
     { text: '测试类型', link: '/testing-types/' },
     { text: '工作流程', link: '/workflows/' },
     { text: '提示词框架', link: '/prompt-frameworks/' },
     { text: '使用示例', link: '/examples/' },
     { text: '指南', link: '/docs/guides/' }
   ]
   ```
   - [ ] 导航菜单已更新
   - [ ] 所有链接正确

5. **添加重定向规则**
   ```typescript
   rewrites: {
     'AccessibilityTesting/:page': 'testing-types/accessibility-testing/:page',
     'AIAssistedTesting/:page': 'testing-types/ai-assisted-testing/:page',
     'APITesting/:page': 'testing-types/api-testing/:page',
     'AutomationTesting/:page': 'testing-types/automation-testing/:page',
     'BugReporting/:page': 'testing-types/bug-reporting/:page',
     'FunctionalTesting/:page': 'testing-types/functional-testing/:page',
     'ManualTesting/:page': 'testing-types/manual-testing/:page',
     'MobileTesting/:page': 'testing-types/mobile-testing/:page',
     'PerformanceTesting/:page': 'testing-types/performance-testing/:page',
     'RequirementsAnalysis/:page': 'testing-types/requirements-analysis/:page',
     'SecurityTesting/:page': 'testing-types/security-testing/:page',
     'TestCaseReviewer/:page': 'testing-types/test-case-reviewer/:page',
     'TestCaseWriting/:page': 'testing-types/test-case-writing/:page',
     'TestReporting/:page': 'testing-types/test-reporting/:page',
     'TestStrategy/:page': 'testing-types/test-strategy/:page',
     'Workflows/:page': 'workflows/:page',
     'Base/PromptExample/:page': 'prompt-frameworks/frameworks/:page'
   }
   ```
   - [ ] 重定向规则已添加
   - [ ] 覆盖所有旧路径

**验收标准**:
- ✅ VitePress 配置已完全更新
- ✅ 所有路径引用正确
- ✅ 重定向规则已添加

---

### 任务 3.2：更新文档链接

**目标**: 更新所有 Markdown 文件中的链接

**优先级**: 🔥 高  
**预计时间**: 4-5 小时  
**负责人**: 开发者 B

#### 具体步骤：

1. **创建链接更新脚本** (`scripts/migrate/06-update-links.sh`)
   ```bash
   #!/bin/bash
   set -e
   echo "🔗 更新文档链接..."
   
   # 更新测试类型模块链接
   find . -name "*.md" -type f -exec sed -i '' 's|/AccessibilityTesting/|/testing-types/accessibility-testing/|g' {} +
   find . -name "*.md" -type f -exec sed -i '' 's|/AIAssistedTesting/|/testing-types/ai-assisted-testing/|g' {} +
   find . -name "*.md" -type f -exec sed -i '' 's|/APITesting/|/testing-types/api-testing/|g' {} +
   find . -name "*.md" -type f -exec sed -i '' 's|/AutomationTesting/|/testing-types/automation-testing/|g' {} +
   find . -name "*.md" -type f -exec sed -i '' 's|/BugReporting/|/testing-types/bug-reporting/|g' {} +
   find . -name "*.md" -type f -exec sed -i '' 's|/FunctionalTesting/|/testing-types/functional-testing/|g' {} +
   find . -name "*.md" -type f -exec sed -i '' 's|/ManualTesting/|/testing-types/manual-testing/|g' {} +
   find . -name "*.md" -type f -exec sed -i '' 's|/MobileTesting/|/testing-types/mobile-testing/|g' {} +
   find . -name "*.md" -type f -exec sed -i '' 's|/PerformanceTesting/|/testing-types/performance-testing/|g' {} +
   find . -name "*.md" -type f -exec sed -i '' 's|/RequirementsAnalysis/|/testing-types/requirements-analysis/|g' {} +
   find . -name "*.md" -type f -exec sed -i '' 's|/SecurityTesting/|/testing-types/security-testing/|g' {} +
   find . -name "*.md" -type f -exec sed -i '' 's|/TestCaseReviewer/|/testing-types/test-case-reviewer/|g' {} +
   find . -name "*.md" -type f -exec sed -i '' 's|/TestCaseWriting/|/testing-types/test-case-writing/|g' {} +
   find . -name "*.md" -type f -exec sed -i '' 's|/TestReporting/|/testing-types/test-reporting/|g' {} +
   find . -name "*.md" -type f -exec sed -i '' 's|/TestStrategy/|/testing-types/test-strategy/|g' {} +
   
   # 更新工作流程链接
   find . -name "*.md" -type f -exec sed -i '' 's|/Workflows/|/workflows/|g' {} +
   
   # 更新 Base 目录链接
   find . -name "*.md" -type f -exec sed -i '' 's|/Base/PromptExample/|/prompt-frameworks/frameworks/|g' {} +
   
   echo "✅ 链接更新完成"
   ```
   - [ ] 链接更新脚本已创建
   - [ ] 脚本权限已设置

2. **执行链接更新脚本**
   ```bash
   bash scripts/migrate/06-update-links.sh
   ```
   - [ ] 脚本执行成功

3. **手动检查关键文件**
   - [ ] README.md 中的链接已更新
   - [ ] README_EN.md 中的链接已更新
   - [ ] 各模块 README 中的相对链接已更新

4. **更新图片和资源链接**
   - [ ] 检查并更新图片链接
   - [ ] 检查并更新其他资源链接

**验收标准**:
- ✅ 所有 Markdown 文件中的链接已更新
- ✅ 相对链接和绝对链接都正确
- ✅ 图片和资源链接正常

---

### 任务 3.3：更新项目配置文件

**目标**: 更新其他配置文件以适应新结构

**优先级**: ⭐ 中  
**预计时间**: 1-2 小时  
**负责人**: 开发者 A

#### 具体步骤：

1. **更新 package.json**
   ```json
   {
     "scripts": {
       "docs:dev": "vitepress dev",
       "docs:build": "vitepress build",
       "docs:preview": "vitepress preview",
       "lint": "markdownlint '**/*.md' --ignore node_modules --ignore .vitepress",
       "link-check": "markdown-link-check **/*.md --ignore node_modules"
     }
   }
   ```
   - [ ] package.json 脚本已更新

2. **更新 .gitignore**
   - 确保包含 .DS_Store 规则
   - 检查其他忽略规则是否需要更新
   - [ ] .gitignore 已检查和更新

3. **创建或更新 CHANGELOG.md**
   - 记录本次重构的变更
   - [ ] CHANGELOG.md 已更新

**验收标准**:
- ✅ 所有配置文件已更新
- ✅ 脚本和工具配置正确
- ✅ 变更日志已记录

---

## 🧪 阶段 4：测试阶段（2-3天）

### 任务 4.1：本地构建测试

**目标**: 确保项目可以正常构建

**优先级**: 🔥 高  
**预计时间**: 2-3 小时  
**负责人**: 测试人员

#### 具体步骤：

1. **清理和重新安装依赖**
   ```bash
   rm -rf node_modules package-lock.json
   npm install
   ```
   - [ ] 依赖重新安装成功

2. **本地开发服务器测试**
   ```bash
   npm run docs:dev
   ```
   - [ ] 开发服务器启动成功
   - [ ] 无错误或警告
   - [ ] 可以在浏览器中访问

3. **构建测试**
   ```bash
   npm run docs:build
   ```
   - [ ] 构建成功
   - [ ] 无错误或警告
   - [ ] 构建产物完整

4. **预览构建结果**
   ```bash
   npm run docs:preview
   ```
   - [ ] 预览服务器启动成功
   - [ ] 构建版本可以正常访问

**验收标准**:
- ✅ 本地开发服务器正常工作
- ✅ 项目构建成功无错误
- ✅ 构建产物可以正常预览

---

### 任务 4.2：链接和内容检查

**目标**: 确保所有链接有效，内容完整

**优先级**: 🔥 高  
**预计时间**: 3-4 小时  
**负责人**: 测试人员

#### 具体步骤：

1. **运行链接检查**
   ```bash
   npm run link-check
   ```
   - [ ] 所有内部链接有效
   - [ ] 没有 404 错误
   - [ ] 外部链接可访问

2. **运行 Markdown 格式检查**
   ```bash
   npm run lint
   ```
   - [ ] Markdown 格式规范
   - [ ] 无格式错误

3. **手动检查关键页面**
   - [ ] 首页正常显示
   - [ ] 测试类型页面可访问
   - [ ] 工作流程页面可访问
   - [ ] 提示词框架页面可访问
   - [ ] 使用示例页面可访问

4. **检查搜索功能**
   - [ ] 搜索功能正常工作
   - [ ] 可以搜索到重命名后的内容
   - [ ] 搜索结果链接正确

5. **检查导航功能**
   - [ ] 侧边栏导航正常
   - [ ] 顶部导航正常
   - [ ] 面包屑导航正常（如果有）

**验收标准**:
- ✅ 所有链接检查通过
- ✅ Markdown 格式检查通过
- ✅ 所有关键页面可正常访问
- ✅ 搜索和导航功能正常

---

### 任务 4.3：功能和兼容性测试

**目标**: 确保所有功能正常，兼容性良好

**优先级**: ⭐ 中  
**预计时间**: 2-3 小时  
**负责人**: 测试人员

#### 具体步骤：

1. **功能测试**
   - [ ] 中英文切换正常
   - [ ] 深色/浅色主题切换正常
   - [ ] 页面加载速度正常
   - [ ] 响应式设计正常

2. **浏览器兼容性测试**
   - [ ] Chrome 浏览器正常
   - [ ] Firefox 浏览器正常
   - [ ] Safari 浏览器正常（如果可能）
   - [ ] Edge 浏览器正常（如果可能）

3. **移动端测试**
   - [ ] 移动端页面显示正常
   - [ ] 移动端导航正常
   - [ ] 触摸操作正常

4. **性能测试**
   ```bash
   # 使用 Lighthouse 或类似工具
   npm install -g lighthouse
   lighthouse http://localhost:4173 --output html --output-path ./lighthouse-report.html
   ```
   - [ ] 性能评分合理
   - [ ] 加载时间可接受
   - [ ] 无明显性能问题

**验收标准**:
- ✅ 所有功能正常工作
- ✅ 主要浏览器兼容性良好
- ✅ 移动端体验良好
- ✅ 性能表现可接受

---

## 🚀 阶段 5：发布阶段（1天）

### 任务 5.1：提交变更

**目标**: 将所有变更提交到版本控制

**优先级**: 🔥 高  
**预计时间**: 1 小时  
**负责人**: 项目负责人

#### 具体步骤：

1. **检查变更状态**
   ```bash
   git status
   git diff --name-only
   ```
   - [ ] 确认所有变更都是预期的
   - [ ] 没有意外的文件变更

2. **添加所有变更**
   ```bash
   git add .
   ```
   - [ ] 所有文件已添加到暂存区

3. **提交变更**
   ```bash
   git commit -m "refactor: restructure project directory

   - Delete empty directories (articles/)
   - Delete system files (.DS_Store)
   - Rename all testing type modules to kebab-case
   - Create new directory structure (docs/, testing-types/, etc.)
   - Move documentation files to docs/
   - Integrate Reference directory content
   - Update all links and configurations
   - Update VitePress configuration

   BREAKING CHANGE: Directory structure has been completely reorganized.
   Old URLs will be redirected to new locations."
   ```
   - [ ] 提交成功
   - [ ] 提交信息清晰完整

4. **推送到远程分支**
   ```bash
   git push origin feature/directory-restructure
   ```
   - [ ] 推送成功

**验收标准**:
- ✅ 所有变更已提交
- ✅ 提交信息规范
- ✅ 远程分支已更新

---

### 任务 5.2：创建 Pull Request

**目标**: 创建 PR 进行代码审查

**优先级**: 🔥 高  
**预计时间**: 1 小时  
**负责人**: 项目负责人

#### 具体步骤：

1. **创建 Pull Request**
   - 标题：`refactor: Restructure project directory for better organization`
   - 描述：包含变更概述、主要变更、新目录结构、测试清单、破坏性变更说明
   - [ ] PR 已创建
   - [ ] 描述完整清晰

2. **添加审查者**
   - [ ] 添加相关审查者
   - [ ] 设置适当的标签

3. **检查 CI/CD 状态**
   - [ ] 自动化构建通过
   - [ ] 所有检查通过

**验收标准**:
- ✅ PR 已创建且描述完整
- ✅ 审查者已添加
- ✅ CI/CD 检查通过

---

### 任务 5.3：代码审查和合并

**目标**: 完成代码审查并合并变更

**优先级**: 🔥 高  
**预计时间**: 2-3 小时  
**负责人**: 项目负责人 + 审查者

#### 具体步骤：

1. **代码审查**
   - [ ] 审查目录结构变更
   - [ ] 审查配置文件变更
   - [ ] 审查链接更新
   - [ ] 审查文档变更

2. **处理审查意见**
   - [ ] 回应所有审查意见
   - [ ] 修复发现的问题
   - [ ] 更新相关文档

3. **合并 PR**
   ```bash
   git checkout main
   git merge feature/directory-restructure
   git push origin main
   ```
   - [ ] PR 已合并
   - [ ] 主分支已更新

**验收标准**:
- ✅ 代码审查完成
- ✅ 所有问题已解决
- ✅ 变更已合并到主分支

---

### 任务 5.4：发布和通知

**目标**: 发布新版本并通知用户

**优先级**: ⭐ 中  
**预计时间**: 1-2 小时  
**负责人**: 项目负责人

#### 具体步骤：

1. **创建版本标签**
   ```bash
   git tag -a v0.2.0 -m "Version 0.2.0: Directory structure refactoring"
   git push origin v0.2.0
   ```
   - [ ] 版本标签已创建
   - [ ] 标签已推送

2. **更新 CHANGELOG.md**
   - [ ] 记录本次发布的所有变更
   - [ ] 说明破坏性变更
   - [ ] 提供迁移指南

3. **发布 GitHub Release**
   - [ ] 创建 GitHub Release
   - [ ] 包含详细的发布说明
   - [ ] 提供迁移指南链接

4. **通知用户**
   - [ ] 在项目首页添加迁移通知
   - [ ] 在社交媒体发布更新通知
   - [ ] 通知主要贡献者和用户

**验收标准**:
- ✅ 新版本已发布
- ✅ 发布说明完整
- ✅ 用户已收到通知

---

## 📊 项目跟踪

### 总体进度

- **总任务数**: 18
- **已完成**: 0
- **进行中**: 0
- **待开始**: 18
- **完成率**: 0%

### 里程碑跟踪

| 里程碑 | 目标日期 | 状态 | 完成标准 |
|--------|---------|------|---------|
| M1: 准备完成 | 第 2 天 | ⏳ 待开始 | 备份完成、脚本就绪 |
| M2: 文件迁移完成 | 第 7 天 | ⏳ 待开始 | 所有文件已移动到新位置 |
| M3: 配置更新完成 | 第 10 天 | ⏳ 待开始 | 所有配置和链接已更新 |
| M4: 测试通过 | 第 13 天 | ⏳ 待开始 | 所有测试项通过 |
| M5: 发布完成 | 第 14 天 | ⏳ 待开始 | 变更已合并并部署 |

### 风险监控

| 风险 | 状态 | 缓解措施 |
|------|------|---------|
| 链接失效 | ⚠️ 监控中 | 自动化链接检查 |
| 构建失败 | ⚠️ 监控中 | 充分测试 |
| 用户书签失效 | ⚠️ 监控中 | 添加重定向规则 |
| 贡献者困惑 | ⚠️ 监控中 | 详细文档和指南 |

---

## 📝 执行说明

### 开始执行

1. **克隆或更新项目**
   ```bash
   git clone <repository-url>
   cd awesome-qa-prompt
   ```

2. **按阶段执行任务**
   - 严格按照阶段顺序执行
   - 完成一个任务后再开始下一个
   - 每个任务完成后更新状态

3. **记录执行过程**
   - 记录遇到的问题和解决方案
   - 记录执行时间和资源消耗
   - 更新任务状态和进度

### 注意事项

1. **备份重要性**
   - 执行任何操作前确保已创建备份
   - 定期检查备份的完整性

2. **测试重要性**
   - 每个阶段完成后进行测试
   - 发现问题立即停止并修复

3. **沟通重要性**
   - 及时与团队成员沟通进度
   - 遇到问题及时寻求帮助

4. **文档重要性**
   - 记录所有变更和决策
   - 更新相关文档

---

**任务清单创建完成，可以开始执行！** 🚀