name: Deploy to GitHub Pages and Custom Domain

on:
  push:
    branches:
      - main
  workflow_dispatch:

# 设置 GITHUB_TOKEN 的权限以允许部署到 GitHub Pages
permissions:
  contents: read
  pages: write
  id-token: write

# 只允许一个并发部署，跳过正在运行和最新队列之间的运行队列
# 但是，不要取消正在进行的运行，因为我们希望允许这些生产部署完成
concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  # 构建任务 - GitHub Pages（使用子路径）
  build-github-pages:
    name: Build for GitHub Pages
    runs-on: ubuntu-latest
    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0 # 获取所有历史记录以支持 lastUpdated
          
      - name: 🟢 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          
      - name: ⚙️ Setup Pages
        uses: actions/configure-pages@v4
        
      - name: 📦 Install dependencies
        run: |
          echo "📦 Installing dependencies..."
          npm ci
          echo "✅ Dependencies installed successfully"
        
      - name: 🏗️ Build VitePress site for GitHub Pages
        env:
          GITHUB_PAGES: 'true'  # GitHub Pages 使用子路径
        run: |
          echo "🏗️ Building VitePress site for GitHub Pages (base: /awesome-qa-prompt/)..."
          npm run docs:build
          echo "✅ Build completed successfully"
        
      - name: 📤 Upload artifact for GitHub Pages
        uses: actions/upload-pages-artifact@v3
        with:
          path: .vitepress/dist

  # 构建任务 - 自定义域名（使用根路径）
  build-custom-domain:
    name: Build for Custom Domain
    runs-on: ubuntu-latest
    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0 # 获取所有历史记录以支持 lastUpdated
          
      - name: 🟢 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          
      - name: 📦 Install dependencies
        run: |
          echo "📦 Installing dependencies..."
          npm ci
          echo "✅ Dependencies installed successfully"
        
      - name: 🏗️ Build VitePress site for Custom Domain
        env:
          GITHUB_PAGES: 'false'  # 自定义域名使用根路径
        run: |
          echo "🏗️ Building VitePress site for Custom Domain (base: /)..."
          npm run docs:build
          echo "✅ Build completed successfully"
        
      - name: 📤 Upload artifact for Custom Domain
        uses: actions/upload-artifact@v4
        with:
          name: custom-domain-artifact
          path: .vitepress/dist
          retention-days: 7

  # 部署任务 - GitHub Pages
  deploy-github-pages:
    name: Deploy to GitHub Pages
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    needs: build-github-pages
    runs-on: ubuntu-latest
    steps:
      - name: 🚀 Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
        
      - name: 🎉 GitHub Pages Deployment complete
        run: |
          echo "🎉 GitHub Pages deployment completed successfully!"
          echo "🌐 GitHub Pages URL: ${{ steps.deployment.outputs.page_url }}"

  # 部署任务 - 自定义域名（可选：如果需要自动部署到 Cloudflare Pages）
  # 注意：如果使用 Cloudflare Pages，需要配置相应的 secrets 和 API token
  # 当前配置仅构建，不自动部署，需要手动部署或配置 Cloudflare Pages 的自动部署
  deploy-custom-domain:
    name: Prepare for Custom Domain Deployment
    needs: build-custom-domain
    runs-on: ubuntu-latest
    steps:
      - name: 📥 Download artifact
        uses: actions/download-artifact@v4
        with:
          name: custom-domain-artifact
          path: .vitepress/dist
      
      - name: 📋 Build Summary
        run: |
          echo "✅ Custom domain build completed!"
          echo "📦 Build artifacts are available in .vitepress/dist"
          echo "🌐 Ready for deployment to: https://qaprompt.inaodeng.com"
          echo ""
          echo "💡 To deploy to Cloudflare Pages:"
          echo "   1. Configure Cloudflare Pages to connect to this repository"
          echo "   2. Set build command: npm run docs:build"
          echo "   3. Set build output directory: .vitepress/dist"
          echo "   4. Set environment variable: GITHUB_PAGES=false"
          echo ""
          echo "💡 Or manually deploy the .vitepress/dist directory to your hosting service"
